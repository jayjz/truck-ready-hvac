# Runtime deps for Streamlit Community Cloud and simple pip installs.
# Keep in sync with [project.dependencies] in pyproject.toml.
# Editable install so `from truck_ready...` works with src/ layout.
-e .
streamlit>=1.35
pydantic>=2.7
fpdf2>=2.8.0
